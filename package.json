{
  "name": "trace",
  "version": "0.1.0",
  "private": true,
  "description": "Trace — an agent-first coding application. By Origin AI.",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "clean": "tsc -b --clean",
    "dev": "pnpm --filter @trace/desktop dev",
    "engine": "pnpm --filter @trace/engine start",
    "lint": "eslint . --ext .ts,.tsx",
    "//format": "`.` rather than a glob: the glob omitted `.mjs`, so the two scripts that hold the build's own checks — the engine smoke test and the contrast test — were the only files Prettier never saw. `.prettierignore` decides what is out of scope now.",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  },
  "prettier": {
    "semi": true,
    "singleQuote": false,
    "printWidth": 100,
    "trailingComma": "all"
  }
}

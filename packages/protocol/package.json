{
  "name": "@trace/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "The Trace engine protocol — shared contract between the engine and every surface (desktop, CLI, web, IDE).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  }
}

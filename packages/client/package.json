{
  "name": "@trace/client",
  "version": "0.1.0",
  "private": true,
  "description": "Surface-agnostic Trace client — the typed engine client, the event reducer, and the view model every surface renders from.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@trace/protocol": "workspace:*"
  }
}
